# Job Prep Backend

Swagger UI доступен после запуска приложения по адресу:

- `/swagger`
- `/swagger/openapi.yaml`

Для защищенных эндпоинтов используется заголовок `Authorization: Bearer <access_token>`.
